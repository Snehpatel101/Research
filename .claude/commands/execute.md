Read CLEANUP_TASKS.md for $ARGUMENTS. Execute changes.

Run `ruff check src/` and verify imports. Use `doc-updater` (Sonnet) for docs.

**Tiered alternatives:**
| Tier | Command | Use When |
|------|---------|----------|
| Light | `/execute-surgical(4c)` | Single focused fix |
| Medium | `/execute-standard(4a)` | Normal task (4 steps) |
| Heavy | `/execute-large(4b)` | Phase-wide work |
