Read CLEANUP_TASKS.md for $ARGUMENTS. Execute changes.

Run `ruff check src/` and verify imports. Use doc-updater subagent for docs.

**For different scopes, use:**
- `/execute-surgical(4c)` - single focused fix
- `/execute-standard(4a)` - normal task (4 steps)
- `/execute-large(4b)` - phase-wide work
