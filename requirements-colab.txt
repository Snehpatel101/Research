# Google Colab dependencies for ML Factory
# Install with: pip install -q -r requirements-colab.txt
#
# Colab pre-installs: numpy, pandas, scipy, scikit-learn, torch, tqdm,
# pyarrow, PyYAML, matplotlib, joblib. We only install what's missing.

# Boosting models
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2.0

# Hyperparameter tuning
optuna>=3.5.0

# Feature engineering
numba>=0.59.0
PyWavelets>=1.5.0

# CLI framework (needed for module imports even if not using CLI directly)
typer>=0.9.0
rich>=13.0.0

# Online drift detection (optional, imported conditionally)
river>=0.21.0
