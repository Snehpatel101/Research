# Google Colab dependencies for ML Factory
# Install with: pip install -q -r requirements-colab.txt
#
# Colab pre-installs: numpy, pandas, scipy, scikit-learn, torch, tqdm,
# pyarrow, PyYAML, matplotlib, joblib. We only install what's missing.

# Pin Colab pre-installed packages to prevent upgrades that break google-colab
pandas==2.2.2

# Boosting models
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2.0

# Hyperparameter tuning
optuna>=3.5.0

# Feature engineering
numba>=0.59.0
PyWavelets>=1.5.0

# CLI framework (needed for module imports even if not using CLI directly)
typer>=0.9.0
rich>=13.0.0

# Online drift detection (river) is NOT included here because
# river>=0.21.0 requires pandas>=2.2.3, which conflicts with
# Colab's pinned pandas==2.2.2. Install separately if needed:
#   pip install river --no-deps
